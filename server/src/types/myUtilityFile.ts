// src/types/myUtilityFile.ts
export const sum = (a: number, b: number): number => a + b;

const myUtilityFunction = (): boolean => true;

export default myUtilityFunction;  // Default export for myUtilityFunction
